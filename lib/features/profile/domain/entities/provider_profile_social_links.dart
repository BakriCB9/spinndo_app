class ProviderProfileSocialLinks {
  int? id;
  String? platform;
  String? url;
  ProviderProfileSocialLinks({this.id, this.platform, this.url}){
     print('baaakr the social links is now ${id} and paltform is ${platform} and the url is ${url}');
  }
}
